# 🔧 Решение ошибки 401 в админ-панели

## 🔴 Проблема
Ошибка `401 (Unauthorized)` при загрузке билетов в админ-панели.

## ✅ Решение

### Шаг 1: Проверьте ADMIN_API_KEY в Vercel

1. Откройте: https://vercel.com/dashboard
2. Проект `vpnwebsite` → **Settings → Environment Variables**
3. Проверьте наличие:
   - **Key:** `ADMIN_API_KEY` (БЕЗ префикса `NEXT_PUBLIC_`)
   - **Value:** `A246123b`
   - **Environments:** Production ✅ Preview ✅ Development ✅

**⚠️ Важно:** 
- Для API routes (server-side) используйте `ADMIN_API_KEY` (БЕЗ префикса)
- НЕ используйте `NEXT_PUBLIC_ADMIN_API_KEY` для этого

### Шаг 2: Перезадеплойте проект

После проверки/добавления переменной:
1. Перейдите в **Deployments**
2. Найдите последний деплой
3. Нажмите **"..." → Redeploy**
4. Подождите завершения (~1-2 минуты)

### Шаг 3: Проверьте вход

1. Откройте: `https://my.outlivion.space/admin/contest`
2. Войдите с паролем: `A246123b`
3. Обновите страницу (Ctrl+Shift+R)

### Шаг 4: Проверьте консоль браузера

Откройте консоль (F12 → Console) и проверьте:
- Есть ли предупреждение `[Admin API] ADMIN_API_KEY not configured`?
- Если есть - переменная не установлена или не читается

## 🔍 Диагностика

Если проблема остается, проверьте логи на Vercel:

1. Vercel Dashboard → Проект → **Deployments**
2. Откройте последний деплой
3. **Functions** → Найдите `/api/admin/contest/participants`
4. Проверьте логи на наличие ошибок

## 📝 Быстрая проверка

Выполните в консоли браузера (F12) после входа:

```javascript
// Проверка сессии
document.cookie.includes('admin_session') // должно быть true

// Проверка запроса
fetch('/api/admin/contest/participants?contest_id=contest-20260117', {
  credentials: 'include'
}).then(r => r.json()).then(console.log)
```

Если видите ошибку - проверьте переменные окружения в Vercel.
