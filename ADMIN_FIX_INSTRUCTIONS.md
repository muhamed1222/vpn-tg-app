# 🔧 Исправление ошибки "Не удалось загрузить конкурс"

## Проблема

Админ-панель не может загрузить данные конкурса, потому что бэкенд требует авторизацию.

## Решение

Нужно настроить `ADMIN_API_KEY` на **ОБОИХ** серверах (бэкенд и фронтенд).

---

## Шаг 1: Настройка на Backend (API сервер)

```bash
# SSH на API сервер
ssh root@72.56.93.135

# Сгенерируйте случайный ключ
openssl rand -base64 32

# Добавьте в .env (скопируйте сгенерированный ключ)
echo "ADMIN_API_KEY=ваш_сгенерированный_ключ" >> /opt/outlivion-api/.env

# Перезапустите API
sudo systemctl restart outlivion-api
```

**Важно:** Запомните этот ключ - он нужен для следующего шага!

---

## Шаг 2: Настройка на Frontend (Vercel)

1. Откройте: https://vercel.com/dashboard
2. Выберите проект `vpnwebsite`
3. Settings → Environment Variables → Add New
4. **Key:** `ADMIN_API_KEY`
5. **Value:** тот же ключ, что вы создали на шаге 1
6. Отметьте: Production ✅ Preview ✅ Development ✅
7. Save → **Redeploy**

---

## ✅ Проверка

После настройки:

1. Подождите 1-2 минуты (деплой Vercel)
2. Откройте `/admin/contest`
3. Введите пароль `A246123b`
4. Должны загрузиться данные конкурса

---

## 🔐 Пример ключа

**Сгенерированный ключ будет выглядеть так:**
```
aB3dEfGhIjKlMnOpQrStUvWxYz1234567890AbCdEf==
```

**Важно:** Используйте тот же ключ на бэкенде и фронтенде!

---

## 🆘 Если всё ещё не работает

1. Проверьте, что ключ одинаковый на обоих серверах
2. Проверьте, что API перезапущен: `sudo systemctl status outlivion-api`
3. Проверьте логи: `journalctl -u outlivion-api -n 50`
4. Очистите кеш браузера (Ctrl+Shift+R)
