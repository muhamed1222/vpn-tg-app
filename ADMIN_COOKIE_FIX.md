# 🔧 Исправление проблемы с cookie в админ-панели

## ⚠️ Важно о httpOnly cookie

**Cookie с флагом `httpOnly: true` НЕ видна в `document.cookie`** - это нормально и правильно для безопасности!

Это означает, что проверка `document.cookie.includes('admin_session')` всегда вернет `false`, даже если сессия установлена.

## ✅ Как проверить, что сессия работает

### Способ 1: Проверка через API

Выполните в консоли браузера:

```javascript
fetch('/api/admin/auth', { credentials: 'include' })
  .then(r => r.json())
  .then(data => console.log('Сессия:', data.authenticated ? '✅ Установлена' : '❌ Не установлена'))
```

Если видите `✅ Установлена` - сессия работает!

### Способ 2: Проверка через Network tab

1. Откройте DevTools (F12)
2. Перейдите в **Network** tab
3. Войдите в админ-панель (введите пароль)
4. Найдите запрос `/api/admin/auth` (POST)
5. Проверьте **Response Headers** - должен быть заголовок `Set-Cookie: admin_session=...`

## 🔄 Что я исправил

1. ✅ Добавил `credentials: 'include'` в запрос входа
2. ✅ Добавил автоматическую перезагрузку страницы после успешного входа
3. ✅ Исправил настройку `secure` cookie для production

## 📋 Инструкция по входу

1. Откройте: `https://my.outlivion.space/admin/contest`
2. Введите пароль: `A246123b`
3. Нажмите "Войти"
4. Страница автоматически перезагрузится
5. После перезагрузки сессия будет активна

## 🐛 Если проблема остается

### Проверка 1: Перезадеплойте проект

После изменений нужно перезадеплоить:
1. Vercel Dashboard → Проект → **Deployments**
2. Нажмите **"..." → Redeploy**
3. Подождите 1-2 минуты

### Проверка 2: Проверьте Network tab

После входа проверьте в Network tab:
- Запрос `/api/admin/auth` (POST) должен вернуть `200 OK`
- В Response Headers должен быть `Set-Cookie: admin_session=...`

### Проверка 3: Проверьте Application tab

1. DevTools (F12) → **Application** tab
2. **Cookies** → `https://my.outlivion.space`
3. Должна быть cookie `admin_session`

## ✅ После исправления

После перезадеплоя и входа:
- Сессия установится автоматически
- Билеты загрузятся без ошибки 401
- Таблица билетов отобразится корректно
