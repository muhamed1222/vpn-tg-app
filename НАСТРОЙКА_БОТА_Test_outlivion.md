# 🤖 Настройка бота @Test_outlivion_bot

## 📋 Информация о боте

- **Username:** `@Test_outlivion_bot`
- **Токен:** `8285323424:AAFslafbTjNMZ0f4TYCRoKBHGbow809KV1g`
- **Тип:** Тестовый бот для разработки

---

## ⚡ Быстрая настройка

### Шаг 1: Настройка переменных окружения сайта

Создайте или обновите файл `.env.local` в корне проекта:

```bash
cd /Users/kelemetovmuhamed/Documents/vpnwebsite
echo "VITE_API_URL=http://localhost:3000" > .env.local
```

Или создайте файл вручную:

```env
# URL API бота (локальная разработка)
VITE_API_URL=http://localhost:3000
```

---

### Шаг 2: Настройка бота

В файле `.env` бота добавьте/обновите:

```env
# Токен бота @Test_outlivion_bot
TELEGRAM_BOT_TOKEN=8285323424:AAFslafbTjNMZ0f4TYCRoKBHGbow809KV1g

# Polling режим для локальной разработки
TELEGRAM_USE_POLLING=1

# Порт API бота
PORT=3000

# CORS - разрешенные домены
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,https://web.telegram.org
```

💡 **Подсказка:** Если у вас есть скрипт `SETUP_TEST_BOT.sh`, запустите его для автоматической настройки.

---

### Шаг 3: Запуск сервисов

**Терминал 1 - Бот:**
```bash
cd /path/to/vpn_bot
npm run dev
```

Дождитесь сообщений:
- `🚀 Server running on port 3000`
- `✅ Polling mode enabled`

**Терминал 2 - Сайт:**
```bash
cd /Users/kelemetovmuhamed/Documents/vpnwebsite
npm run dev
```

Дождитесь сообщения:
- `Local: http://localhost:5173/`

---

### Шаг 4: Настройка WebApp URL в Telegram

#### Вариант A: Через @BotFather (рекомендуется)

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/mybots`
3. Найдите `@Test_outlivion_bot` в списке
4. Выберите бота → `Bot Settings` → `Menu Button`
5. Выберите `Configure menu button`
6. Введите текст кнопки: `Открыть`
7. Выберите `Web App`
8. Введите URL:
   - **Для локальной разработки:** нужен туннель (см. ниже)
   - **Для тестирования:** можно использовать временный туннель

#### Вариант B: Использование туннеля для локальной разработки

```bash
# Установите ngrok (если еще не установлен)
brew install ngrok

# Запустите туннель на порт 5173
ngrok http 5173
```

Скопируйте HTTPS URL (например: `https://abc123.ngrok.io`) и используйте его в BotFather.

⚠️ **Важно:** URL должен начинаться с `https://` для работы в Telegram!

---

### Шаг 5: Тестирование

1. **Откройте бота в Telegram:**
   - Найдите `@Test_outlivion_bot` в поиске Telegram
   - Или перейдите напрямую: https://t.me/Test_outlivion_bot

2. **Отправьте команду `/start`**

3. **Откройте WebApp:**
   - Нажмите на кнопку меню бота (если настроено)
   - Или используйте кнопку WebApp в ответе бота

4. **Проверьте авторизацию:**
   - WebApp должен открыться внутри Telegram
   - Нажмите "Продолжить с Telegram"
   - Должна произойти автоматическая авторизация
   - Вы попадете на страницу аккаунта

---

## 🔍 Проверка работы

### В консоли браузера (F12)

Откройте консоль разработчика и проверьте:

```javascript
// Проверка Telegram WebApp
console.log('WebApp доступен:', !!window.Telegram?.WebApp);
console.log('initData:', window.Telegram?.WebApp?.initData);

// Проверка пользователя
console.log('Пользователь:', window.Telegram?.WebApp?.initDataUnsafe?.user);
```

### В Network вкладке

1. Откройте вкладку Network в DevTools
2. Найдите запрос к `/api/me`
3. Проверьте заголовки:
   - Должен быть заголовок `Authorization` с `initData`
4. Проверьте ответ:
   - Должен содержать данные пользователя

---

## ❌ Решение проблем

### Проблема: "Telegram WebApp не инициализирован"

**Причина:** Сайт открыт не через Telegram.

**Решение:**
- ✅ Откройте сайт через кнопку WebApp в Telegram боте
- ❌ Не открывайте в обычном браузере

### Проблема: CORS ошибка

**Причина:** Домен не добавлен в `ALLOWED_ORIGINS`.

**Решение:**
1. Откройте `.env` файл бота
2. Добавьте домен в `ALLOWED_ORIGINS`:
   ```env
   ALLOWED_ORIGINS=http://localhost:5173,https://ваш-туннель.ngrok.io,https://web.telegram.org
   ```
3. Перезапустите бота

### Проблема: 401 Unauthorized

**Причина:** `initData` не валидируется ботом.

**Решение:**
1. Проверьте, что в боте настроена валидация `initData`
2. Убедитесь, что используется правильный токен бота для валидации
3. Проверьте логи бота на наличие ошибок

### Проблема: Бот не отвечает

**Причина:** Бот не запущен или не получает сообщения.

**Решение:**
1. Проверьте, что бот запущен: `npm run dev`
2. Проверьте логи в терминале
3. Убедитесь, что `TELEGRAM_USE_POLLING=1` в `.env` бота
4. Проверьте правильность токена

---

## 📝 Чек-лист настройки

Перед тестированием убедитесь:

- [ ] Файл `.env.local` создан с правильным `VITE_API_URL`
- [ ] Бот настроен с токеном `8285323424:AAFslafbTjNMZ0f4TYCRoKBHGbow809KV1g`
- [ ] `TELEGRAM_USE_POLLING=1` в `.env` бота
- [ ] `ALLOWED_ORIGINS` содержит нужные домены
- [ ] Бот запущен и отвечает на `/start`
- [ ] Сайт запущен на порту 5173
- [ ] WebApp URL установлен в BotFather
- [ ] Сайт доступен по HTTPS (для продакшна или через туннель)

---

## 🔗 Полезные ссылки

- Бот в Telegram: https://t.me/Test_outlivion_bot
- BotFather: https://t.me/BotFather
- Подробная инструкция: `НАСТРОЙКА_ВХОДА_ЧЕРЕЗ_БОТА.md`

---

## 💡 Дополнительные команды

### Проверка статуса бота

```bash
# Проверка доступности API бота
curl http://localhost:3000/health

# Должно вернуть: OK
```

### Очистка кеша

```bash
# Очистка кеша npm
npm cache clean --force

# Переустановка зависимостей
rm -rf node_modules package-lock.json
npm install
```

---

✅ **Готово!** Теперь бот @Test_outlivion_bot настроен и готов к работе.

