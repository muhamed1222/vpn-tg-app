# 🔧 Исправление проблемы с сессией в админ-панели

## ✅ Переменные установлены

Вижу, что в Vercel установлены:
- ✅ `ADMIN_API_KEY` = `A246123b`
- ✅ `ADMIN_PASSWORD` = `A246123b`

## 🔄 Что нужно сделать СЕЙЧАС

### 1. Перезадеплойте проект

**Важно:** После установки переменных окружения проект нужно перезадеплоить!

1. В Vercel Dashboard → Проект `vpnwebsite`
2. Перейдите в **Deployments**
3. Найдите последний деплой
4. Нажмите **"..." → Redeploy**
5. Подождите завершения (~1-2 минуты)

### 2. Очистите кеш браузера

После деплоя:
1. Откройте админ-панель: `https://my.outlivion.space/admin/contest`
2. Нажмите **Ctrl+Shift+R** (или **Cmd+Shift+R** на Mac) для жесткой перезагрузки
3. Или откройте в режиме инкогнито

### 3. Войдите заново

1. Введите пароль: `A246123b`
2. Нажмите "Войти"
3. Должна установиться сессия

## 🔍 Проверка работы

После входа откройте консоль браузера (F12 → Console) и проверьте:

```javascript
// Проверка сессии
document.cookie.includes('admin_session') // должно быть true
```

Если `true` - сессия установлена. Если `false` - проблема с cookie.

## 🐛 Если проблема остается

### Проверка 1: Логи на Vercel

1. Vercel Dashboard → Проект → **Deployments**
2. Откройте последний деплой
3. **Functions** → Найдите `/api/admin/contest/participants`
4. Проверьте логи - должны быть сообщения:
   - `[Admin API] Using admin session with API key` (если все ОК)
   - `[Admin API] ADMIN_API_KEY not configured` (если проблема)

### Проверка 2: Проверьте переменные

Убедитесь, что переменные установлены для **Production** окружения:
- В Vercel: Settings → Environment Variables
- Проверьте, что `ADMIN_API_KEY` и `ADMIN_PASSWORD` отмечены для **Production** ✅

## 📝 Изменения, которые я сделал

1. ✅ Добавил `credentials: 'include'` в fetch запросы
2. ✅ Исправил настройку `secure` cookie для production
3. ✅ Добавил логирование для отладки

Все изменения отправлены в git и должны автоматически задеплоиться.

## ⏱️ Время ожидания

После перезадеплоя подождите **1-2 минуты**, затем:
1. Очистите кеш браузера (Ctrl+Shift+R)
2. Войдите заново
3. Проверьте работу
